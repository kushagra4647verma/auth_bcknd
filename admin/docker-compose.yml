version: "3.9"

services:
  gateway:
    build: ./gateway
    container_name: admin-gateway
    ports:
      - "3100:3100"
    env_file: .env
    depends_on:
      - admin-service

  admin-service:
    build: ./services/admin
    container_name: admin-service
    env_file: .env
